import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  biggerText: {
    fontSize: 40,
    fontWeight: '700',
    marginTop: 25,
  },
  input: {
    borderColor: '#ccc',
    width: '100%',
    height: 50,
    backgroundColor: '#eee',
  },
});

export default styles;
